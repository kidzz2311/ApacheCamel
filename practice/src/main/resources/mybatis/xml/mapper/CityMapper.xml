<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mybatis.mapper.CityMapper">
    <resultMap id="CityResultMap" type="City">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="state" column="state"/>
        <result property="country" column="country"/>
    </resultMap>

    <select id="findById" resultMap="CityResultMap">
        select id, name, state, country from city where id = #{id}
    </select>
    <select id="findByState" resultMap="CityResultMap">
        select id, name, state, country from city where state = #{state}
    </select>
    <select id="findByName" resultMap="CityResultMap">
        select id, name, state, country from city where lower(name) like '%${name}%'
    </select>
    <select id="findAll" resultMap="CityResultMap">
        select id, name, state, country from city
    </select>
    <select id="selectComplexCity" resultType="City">
        select * from city where lower(name) like '%${name}%' or state = #{state}
    </select>
    <insert id="insertCity" parameterType="City">
        insert into city (name, state, country) values (#{name}, #{state}, #{country})
    </insert>
    <update id="updateCity" parameterType="City">
        update city set name = #{name}, state = #{state}, country = #{country} where id = #{id}
    </update>
</mapper>